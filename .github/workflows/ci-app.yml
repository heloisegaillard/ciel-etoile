name: CI — Application AeroPlan Voyages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-app:
    runs-on: ubuntu-latest

    steps:
      - name: Récupération du code
        uses: actions/checkout@v3

      - name: Installation de Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Installation des dépendances
        run: |
          pip install --upgrade pip
          pip install streamlit requests python-dotenv pytest

      - name: Lancement des tests de la logique métier
        run: |
          pytest tests/test_app_logic.py -v

  build-package:
    runs-on: ubuntu-latest
    needs: test-app

    steps:
      - name: Récupération du code
        uses: actions/checkout@v3

      - name: Construction de l'image Docker
        run: |
          docker build -t aeroplan-app .

      - name: Vérification de l'image construite
        run: |
          docker images aeroplan-app
